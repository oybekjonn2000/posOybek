# ========================================================
# RESTAURANT POS API - DEV PROFILE
# Faollashtirish: spring.profiles.active=dev
# ========================================================

# Database (dev local)
spring.datasource.url=jdbc:postgresql://localhost:5432/restaurant_pos_dev
spring.datasource.username=pos_user
spring.datasource.password=pos_dev_pass

# JPA - SQL loglarni ko'rish uchun
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT (faqat dev uchun)
app.jwt.secret=dev-secret-key-not-for-production-use-only-in-development-environment

# Device
app.device.id=DEV-001
app.device.name=Development-POS

# Logging (batafsil)
logging.level.com.restaurantpos=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
